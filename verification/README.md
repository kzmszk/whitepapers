# 現在の検証記録

全5テーマの記事追加の最新結果は [articles.md](articles.md)。以下は初期調査・構成改訂時点の記録で、4ファイル構成や未執筆テーマの記載は当時の状態。

# 検証記録

確認日：2026-09-22。ローカル成果物を対象に実施。

## 政策の考え方を軸にした構成改訂

同日、トップ・5冊の要約・エネルギー解説・成果確認の指標案・データ比較の順に試作を改訂。確認は実ブラウザで実施した。

- トップの主導線から白書別の要約を開き、課題・対応・出典を確認。
- テーマから中小企業白書の要約を開き、Enterで閉じ、同じハッシュのリンクから再度開けることを確認。
- エネルギー白書の要約から解説へ進み、S＋3Eの図解、具体策、暮らしとの関係、分析画面まで確認。
- PCで開始2022年・前年比・総合CPI追加を操作。2025年の値が−13.2%、3.6%、3.2%となることを確認。
- スマートフォン相当で白書要約の開閉、記事内の図解、比較の期間・系列変更、表の展開を実施。2022年=100の2025年値は65.7、101.1、109.5。画面幅とページ幅はともに375px。グラフと表を確認し、表は枠内で横スクロールできる構造を維持。
- 標準表示に戻し、比較を初期化。2025年の196.8、123.0を再確認。標準画面幅・ページ幅はともに906px、アプリのエラー・警告ログなし。
- HTMLの重複IDと内部リンク14件・ラベル参照の不整合なし。白書要約は5件。JavaScriptの構文検査成功。
- 長いページ内をすぐ移動できるよう、アンカー移動のアニメーションを外した。スマートフォンの見出しと図中ラベルの改行を調整。

今回の改訂は比較の計算・CSV/JSON生成処理とデータを変更していない。ダウンロード内容の照合は下記の初回検証を参照。新たな独立レビュー、本番公開、200%文字拡大は今回実施していない。

## 原資料と再生成

- `scripts/assemble_catalog.py`：台帳97項目、保存済み80ファイルの存在とSHA-256一致を確認。掲載場所のみ確認・取得失敗等17項目を保存済み扱いにしていない。
- `scripts/trade_normalize.py`：47暦年を再生成。品目表と輸入総額38年、別形式の公式HTMLと輸出入46年が一致。
- `scripts/energy_normalize.py`：35年度×20系列＝700行を再生成。年度・項目名・単位と、部門合計・供給内訳合計を確認。親担当でも公式PDF7頁の16.3%と定義を目視確認。
- `scripts/industry_normalize.py`：155分類×2系列×8年＝2,480行を再生成。主要32年次値と月次原指数の12か月平均を丸め誤差内で照合。
- `scripts/build_demo_data.py`：CPI80か月から完全年2020〜2025の3系列を再集計。2026年は8か月のため除外。2020年平均と100との差は0.001未満。
- 別の担当がCPI原本を独立集計し、18値と計算式を確認。欠測の空文字と計算条件の情報不足を修正・再確認済み。詳細は `data-review.md`。

## 実ブラウザで確認した操作

ローカルHTTPサーバーで `prototype/` を配信し、実際のブラウザ画面を操作した。

1. 初期表示：2020〜2025年、燃料輸入額とエネルギーCPI。2025年は196.8と123.0。
2. 開始年を2022へ変更し、前年比表示に切り替え、総合CPIを追加。表の2025年値は−13.2、3.6、3.2。
3. CSV保存ボタンを押し、実ダウンロードされたファイルを読み戻した。3系列×4年＝12観測、表示値と表の一致、出典URLとハッシュの保存を確認。`browser-export.csv` に保存。
4. 計算条件保存ボタンを押し、実ダウンロードされたJSONを読み戻した。系列定義、品目コード、単位、集計方法、原本ハッシュを確認。`browser-recipe.json` に保存。
5. 全系列の選択を外すと説明を表示し、ダウンロードボタンを無効化。
6. エネルギーCPIだけを選んで前年比に切り替えると、2019年未収録による2020年の値を「欠測」と表示。ゼロ表示しない。
7. モバイル相当幅を指定して画面確認。実際のページ有効幅375px、ページ全体の幅375pxで横にはみ出しなし。グラフ301px幅、文字を縮小せず描画。期間と系列の操作欄・軸・説明文を目視確認。
8. デスクトップ幅に戻して再読込。2022年=100の2025年値は燃料65.7、エネルギーCPI101.1。ブラウザのエラー・警告ログなし。

ブラウザのダウンロードイベント待機はタイムアウトしたが、ファイルそのものは保存されており、その内容を読み戻して検証した。イベント待機だけを成功の根拠としていない。

## 構文と配信境界

- `node --check prototype/app.js` 成功。
- `wrangler.jsonc` をJSONとして読み込み、配信対象が `prototype/` に限られることを確認。
- 配信フォルダは試作のHTML・CSS・JavaScript・JSONの4ファイルに限定。参考画像と白書原PDFは含めていない。

## 未検証

Cloudflare上のビルド・デプロイ、R2/D1接続、実負荷、費用、全スクリーンリーダー、200%文字拡大は未検証。白書全文・全図表との一致も網羅的には未検証。対象とする本番記事が決まった段階で、本文・図表・出典と取得版を照合する。
